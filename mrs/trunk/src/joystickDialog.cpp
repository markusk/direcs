#include "joystickDialog.h"

JoystickDialog::JoystickDialog(QWidget* parent, Qt::WFlags fl) : QDialog(parent, fl)
{
	ui.setupUi(this);
}

/*
void JoystickDialog::on_btnAboutQt_clicked(bool checked)
{
	Q_UNUSED(checked);

	QApplication::aboutQt();
}
*/
